#include "a.h"




void* zalloc(size_t size)
{
    void* p = malloc(size);
    memset(p, 0, size);
    return p;
}























































































